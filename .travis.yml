language:
  - "python"

python:
  - "3.3"

install:
  -  "pip install pytest"

script:
  -  "git clone https://github.com/django/django.git --depth=1"
  -  "mkdir ~/dev"
  -  "mv django ~/dev/django"
  -  "./run_tests.sh"

notifications:
  email:
    recipients:
      - papaloizouc@gmail.com
    on_success: change
    on_failure: always
